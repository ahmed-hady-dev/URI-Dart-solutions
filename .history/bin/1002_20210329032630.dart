import 'dart:io';

void main() {
  double A, R;
  R = d.parse(stdin.readLineSync());
  double n = 3.14159;
  A = n * R * R;

  // B =int.parse(stdin.readLineSync());
  // X = A+B;
  // print('X = $X');
}
