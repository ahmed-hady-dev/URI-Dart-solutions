import 'dart:io';

void main() {
  double A, R;
  double n = 3.14159;
  A = n * R * R;
  R = int.parse(stdin.readLineSync());
  // B =int.parse(stdin.readLineSync());
  // X = A+B;
  // print('X = $X');
}
