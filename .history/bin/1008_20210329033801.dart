import 'dart:io';

void main() {
  int employeeNumber, workedHours;
  double amountHeReceived;

  // A = int.parse(stdin.readLineSync());
  // B = int.parse(stdin.readLineSync());
  // C = int.parse(stdin.readLineSync());
  // D = int.parse(stdin.readLineSync());

  // sum = ((A * B) - (C * D));

  // print('DIFERENCA = $sum');
}
